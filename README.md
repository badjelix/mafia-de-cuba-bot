# Mafia de Cuba Bot for Discord


## Para o Chalupa

#### Como sacar do repositorio:

```bash
git pull
```

#### Como meter no repositorio:

```bash
git add <[nomes dos ficheiros] ou .>
git commit -m '<mensagem>'
git push
```
